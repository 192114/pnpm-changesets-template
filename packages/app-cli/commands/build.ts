// import shell from 'shelljs'

// export async function build(projectName: string, configPath: string): Promise<void> {
//   if (projectName) {
//     // 清理对应项目的包
//     shell.exec(`pnpm --filter ${projectName} run build`)
//   }
// }
